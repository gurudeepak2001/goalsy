I am uploading the Figma file again.

Perform a complete Figma accuracy correction pass across the entire Goalsy Credit application.

The current website has the same types of mistakes across multiple pages:

* spacing differences
* elements out of position
* incorrect sizing
* missing details
* incorrect alignment
* components not matching Figma
* some features missing
* buttons/interactions not fully working

Use the uploaded Figma file as the ONLY source of truth.

Do not redesign anything.
Do not create your own layout.
Do not simplify the UI.

The goal:
Make the website look and behave like the Figma design converted into a real application.

---

# Step 1 — Analyze Before Editing

First inspect:

* Every Figma page
* Every frame
* Every component
* Every section
* Every interaction
* Every asset

Compare the current website against Figma.

Identify:

* missing features
* incorrect components
* visual differences
* functionality problems

Then fix them.

---

# Step 2 — Global Layout Corrections

Apply corrections across ALL pages:

Fix:

## Positioning

* Align every element exactly like Figma.
* Correct items that are too high, too low, left, or right.
* Maintain consistent margins.

## Spacing

Match Figma:

* padding
* gaps
* section spacing
* card spacing
* text spacing

## Sizing

Correct:

* card dimensions
* buttons
* icons
* images
* input fields
* navigation elements

---

# Step 3 — Global Component Corrections

Review all reusable components:

Header:

* logo position
* title alignment
* avatar placement

Bottom Navigation:
Must match Figma exactly:

TODAY
GOALS
CALENDAR
AI
PROFILE

Important:

* AI must be a navigation feature.
* Goalsy Score should NOT be a bottom navigation item.
* Goalsy Score opens from Profile.

Buttons:
Fix:

* size
* position
* radius
* typography
* states

Cards:
Fix:

* dimensions
* radius
* colors
* borders
* shadows
* internal spacing

Toggle switches:
Fix:

* size
* alignment
* position
* active/inactive appearance

---

# Step 4 — Feature Completeness

Make sure every Figma feature exists:

Include:

* all sections
* all cards
* all buttons
* all icons
* all charts
* all progress indicators
* all score displays
* all settings
* all interactions

If it appears in Figma, it must appear in the website.

---

# Step 5 — Functionality Testing

Test every page:

Verify:

Navigation:

* Splash → Welcome
* Welcome → Sign In/Create Account
* Create Account → Financial Connection
* Financial Connection → Goals Overview
* Bottom navigation works
* Profile → Goalsy Score works

Buttons:

* Every button has an action.
* Every clickable element works.

Forms:

* Inputs work.
* Validation states work.

---

# Step 6 — Final Side-by-Side Review

For every screen:

Compare:
Current website vs Figma

Fix:

* visual differences
* missing features
* alignment problems
* inconsistent components

The final result should be:

1. Visually matching Figma as closely as possible.
2. Fully functional.
3. Consistent across all pages.
4. Production-ready.

Do not stop after fixing one page.
Apply the corrections across the entire application.
