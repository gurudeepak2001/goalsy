I need a complete technical explanation of how the email/phone verification on the login and signup screens works in this project. Do not change any code — this is a read-only investigation. Answer by reading the actual files and quoting them.
Please cover:
    1    What method is actually used — is it a one-time password (OTP) code the user types in, or an email confirmation link they tap? If both exist anywhere in the codebase, describe both and tell me which one the login/signup flow uses today.
    2    Which service or API sends it — the exact provider and product (e.g. Firebase Auth email link, Firebase sendEmailVerification, Twilio Verify, SendGrid, Resend, AWS SES, Supabase, a custom Nodemailer setup). Give me the package names and versions from package.json, and the exact SDK function calls used.
    3    The full flow, step by step — from the user tapping "Create Account" / "Sign In" to the account becoming verified. Name the actual files and functions at each step (client screen → API route / Cloud Function → provider → callback → where the verified flag is stored).
    4    Where verification state lives — which field on which collection/table records that a user is verified, and how the client re-checks it after the user completes verification.
    5    If it's an OTP: how the code is generated, how many digits, where it's stored, how long it's valid, how it's compared on submit, whether there's rate limiting or a resend cooldown, and how many wrong attempts are allowed before lockout.
    6    If it's a link: the exact sender address, the domain it sends from, whether a custom domain / custom SMTP is configured or it uses the provider's default, and any deliverability settings (SPF, DKIM, DMARC) that are or are not set up.
    7    All environment variables and secrets involved, by name only — do not print the values.
    8    Any provider dashboard configuration that isn't visible in the code but is needed for this to work (email templates, authorised domains, verification service SIDs, etc.).
Finally: our verification emails are landing in the spam folder. Based on what you find, list the specific reasons that is happening in this setup, and tell me what would need to change to switch this flow from an email link to a 6-digit OTP code instead — which provider you'd use, what would need to be added, and what would break.