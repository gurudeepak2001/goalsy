We've been debugging the __clerk_db_jwt persistence fix across many rebuild cycles, and each fix has addressed one symptom (hang, then crash, then AbortError) without fixing the underlying issue: after force-kill, the app still lands on Welcome instead of Goals, meaning the persisted token still isn't successfully resuming the Clerk session.

I can't reliably capture live console logs from Safari Web Inspector on cold-start anymore — the WebView process is fully replaced on relaunch, and Web Inspector can't reattach fast enough to catch the earliest logs. So going forward, I need you to make the code self-diagnosing rather than relying on me watching a live console.

Please do the following:

Read through the entire token persistence flow yourself, end to end — the save (extracting __clerk_db_jwt from outgoing FAPI request URLs), the storage (Preferences), and the restore/injection path (module-level preload, the /v1/client-only fetch injection, the AbortError handling). Trace the actual logic as if you were the browser executing it, and identify anywhere the token could be getting saved incorrectly, read back incorrectly, or injected into a request in a way Clerk's server would reject (wrong param name, wrong encoding, stale/expired token, wrong request shape).
Add persistent, file-based/Preferences-based diagnostic logging that survives the WebView process being killed — write a debug_last_injection entry to Preferences after every injection attempt, containing: timestamp, whether the token was found in storage, the token's length, the exact request URL that was built, the response status if the fetch completed, the response body's client_id and sessions count if parseable, and the full error name/message/stack if it failed. This should be write-once-per-attempt, not something that needs a live console to see.
Write a small standalone script or test you can run yourself (in Node, or via curl/fetch simulation) that replicates what the cold-start injection is supposed to do: take a real persisted __clerk_db_jwt value (you can use a placeholder or ask me to paste one from a previous successful save), construct the exact request the app would build, and check whether Clerk's actual API accepts it — independent of the iOS app entirely. This tests the server-side logic without needing my phone at all.
Once you've identified and fixed the actual root cause (not just made failures softer), push the change and tell me clearly: what the root cause was, why previous attempts didn't catch it, and exactly what changed.
When I test next, I'll force-kill and reopen once, then tell you Goals or Welcome. If it's still Welcome, I'll pull the debug_last_injection value from Preferences (tell me how — e.g., a temporary debug screen or button, or a Preferences dump you add) so we get the full diagnostic without needing live Web Inspector timing.

Please do all the investigation, reasoning, and testing you can do yourself right now before pushing anything — I'd rather you spend the time finding the actual bug than have me run another cycle to catch a log line.