Perform a complete UI correction pass on the entire Goalsy Credit application.

The current implementation has many visual differences from the Figma design.

Use the Figma file as the ONLY source of truth.

Do not redesign anything.
Do not create a new style.
Do not simplify the layouts.

The goal:
Make every page look like the Figma design and fix all alignment/layout problems.

---

## Fix All Pages

Review every screen and correct:

* Incorrect positioning
* Misaligned elements
* Wrong spacing
* Wrong sizing
* Incorrect margins and padding
* Elements overlapping each other
* Buttons placed on top of other buttons
* Text overlapping
* Cards not aligned correctly
* Icons in the wrong position
* Components not matching Figma

---

## Layout Corrections

For every page:

Compare the current website with Figma and fix:

### Buttons

* Make sure buttons are not overlapping.
* Correct button placement.
* Match Figma button size.
* Match spacing between buttons.
* Fix text alignment inside buttons.
* Fix border radius and styling.

### Cards

* Correct card width and height.
* Align cards properly.
* Fix internal padding.
* Match Figma spacing.
* Ensure no content is cut off.

### Text

Fix:

* Font size
* Font weight
* Line spacing
* Text position
* Alignment

### Icons and Images

Fix:

* Icon size
* Icon position
* Image alignment
* Spacing between icons and text

---

## Responsive Layout Check

Make sure:

* Elements do not overlap on mobile screens.
* Buttons stay in the correct position.
* Cards resize correctly.
* Text does not break the layout.
* Navigation stays aligned.

---

## Component Consistency

Review shared components:

* Header
* Bottom Navigation
* Buttons
* Cards
* Input fields
* Toggle switches
* Progress bars
* Score rings

Make sure all pages use consistent components matching Figma.

---

## Functionality Check

While fixing the UI, keep all functionality working:

* Buttons should still work.
* Navigation should still work.
* Forms should still work.
* Clickable cards should still work.

Do not remove existing features.

---

## Final Requirement

Go through every page one by one:

1. Compare with Figma.
2. Fix all visual differences.
3. Fix all alignment issues.
4. Fix overlapping elements.
5. Confirm the page matches the Figma layout.

Do not stop after fixing one screen.

Apply these corrections to the entire application.

The final website should look like the Figma design converted into a working product.
